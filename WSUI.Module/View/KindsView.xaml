<UserControl x:Class="WSUI.Module.View.KindsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                              HorizontalContentAlignment="Stretch"
             >
    <DockPanel HorizontalAlignment="Stretch" x:Name="panel" Margin="40 0 0 0">
        
        <ItemsControl ItemsSource="{Binding Path=KindsCollection,UpdateSourceTrigger=PropertyChanged}" 
                      HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" IsEnabled="{Binding Enabled,UpdateSourceTrigger=PropertyChanged}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ToggleButton Command="{Binding Path=ChooseCommand}" Content="{Binding Path=UIName}"
                                  HorizontalContentAlignment="Right" Margin="0 2 0 2" FontSize="16" Width="{Binding ElementName=panel,Path=Width}"
                                  IsChecked="{Binding Toggle,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
